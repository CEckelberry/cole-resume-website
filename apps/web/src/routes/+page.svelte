<script lang="ts">
  // Phase 0 placeholder. Real hero lands in Task 1.2.
</script>

<main class="placeholder">
  <h1>cole.eckelberry</h1>
  <p class="eyebrow">portfolio · 2026 · phase 0</p>
</main>

<style>
  .placeholder {
    min-height: 100dvh;
    display: grid;
    place-items: center;
    gap: var(--space-4);
    padding: var(--space-6);
    text-align: center;
  }

  h1 {
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: var(--type-display);
    line-height: var(--type-display-lh);
    letter-spacing: -0.02em;
    color: var(--text-primary);
  }

  p {
    color: var(--text-tertiary);
  }
</style>
